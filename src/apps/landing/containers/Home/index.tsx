import { FC } from 'react';
import { Header, Hero, Offer, Products } from '@landing/components';

const Home: FC = () => (
  <div>
    <Header />
    <Hero />
    <Offer />
    <Products />
  </div>
);

export default Home;
