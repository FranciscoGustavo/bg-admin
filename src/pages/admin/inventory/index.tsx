import { FC } from 'react';
import { LayoutAdmin } from '@admin/components';

const AdminInventoryPage: FC = () => (
  <LayoutAdmin>
    <h1>Inventario</h1>
  </LayoutAdmin>
);

export default AdminInventoryPage;
